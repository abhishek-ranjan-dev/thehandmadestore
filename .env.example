# The marketing site currently needs no environment variables.
#
# Copy this file to .env.local (gitignored) if you add any. It is kept as a
# placeholder so the convention is obvious when the site later needs secrets.
